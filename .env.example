# Frontend env (used at build time)
# Leave empty when frontend + backend run on the same server (recommended)
# VITE_API_URL=

# If frontend is hosted separately (Vercel/Netlify), set your backend URL:
# VITE_API_URL=https://your-backend.example.com
